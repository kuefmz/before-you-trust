use_default_settings: true

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  # Required by SearXNG. Override this with SEARXNG_SECRET outside local dev.
  secret_key: "before-you-trust-local-change-me"
  limiter: false
